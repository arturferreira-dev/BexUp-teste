const soma = () => 2;

describe("test", () => {
  it("should be able to returns true", () => {
    expect(soma()).toBe(2);
  });
  it("should be able to returns true", () => {
    expect(false).toBe(false);
  });
});
